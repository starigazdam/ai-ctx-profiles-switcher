# Generated by ctx — do not commit
# Legacy: settings.local.json is no longer written by ctx as of the
# COPILOT_HOME isolation feature (issue #1); this entry is kept for repos
# still on an older ctx version, or that haven't cleaned up a leftover file.
.github/copilot/settings.local.json
*.code-workspace
.vscode/

# COPILOT_HOME per-context cache dirs ctx creates under ~/.config/ctx/homes/
# (or $CTX_HOMES_ROOT) live outside any project repo by default, so nothing
# needs to be ignored here for them. If you ever point CTX_HOMES_ROOT at a
# path inside a repo, ignore it explicitly, e.g.:
# .config/ctx/homes/
